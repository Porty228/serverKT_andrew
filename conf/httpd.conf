
Define CAMPUS_ROOT "C:/Users/usdth/Documents/VSC/campus"
Define APACHE_ROOT "C:/Apache24"

ServerRoot "${APACHE_ROOT}"
ServerAdmin admin@campus.local
ServerName campus.local

Timeout 60
KeepAlive On
MaxKeepAliveRequests 100
KeepAliveTimeout 5


PidFile "${CAMPUS_ROOT}/logs/httpd.pid"

LoadModule authn_file_module modules/mod_authn_file.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_host_module modules/mod_authz_host.so
LoadModule authz_groupfile_module modules/mod_authz_groupfile.so
LoadModule authz_user_module modules/mod_authz_user.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule access_compat_module modules/mod_access_compat.so
LoadModule auth_basic_module modules/mod_auth_basic.so
LoadModule reqtimeout_module modules/mod_reqtimeout.so
LoadModule filter_module modules/mod_filter.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule env_module modules/mod_env.so
LoadModule headers_module modules/mod_headers.so
LoadModule setenvif_module modules/mod_setenvif.so
LoadModule version_module modules/mod_version.so
LoadModule ssl_module modules/mod_ssl.so
LoadModule autoindex_module modules/mod_autoindex.so
LoadModule dir_module modules/mod_dir.so
LoadModule alias_module modules/mod_alias.so
LoadModule rewrite_module modules/mod_rewrite.so


DocumentRoot "${CAMPUS_ROOT}/htdocs"
DirectoryIndex index.html index.htm


<Directory />
    AllowOverride None
    Require all denied
</Directory>

<Directory "${CAMPUS_ROOT}/htdocs">
    Options FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>


<IfModule mime_module>
    TypesConfig "${APACHE_ROOT}/conf/mime.types"
    

    AddType text/markdown .md
    
    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz
</IfModule>


<IfModule headers_module>

    Header always set X-Project "TechCampus"
</IfModule>

<IfModule log_config_module>

    LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %D" campus_combined
    LogFormat "%h %l %u %t \"%r\" %>s %b %D" campus_common
    

    ErrorLog "${CAMPUS_ROOT}/logs/error.log"
    CustomLog "${CAMPUS_ROOT}/logs/access.log" campus_combined
</IfModule>

LogLevel warn


ErrorDocument 404 /errors/404.html
ErrorDocument 500 /errors/500.html
ErrorDocument 410 "Gone - This resource is no longer available."


Alias /static/ "${CAMPUS_ROOT}/assets/"
<Directory "${CAMPUS_ROOT}/assets">
    Options -Indexes
    AllowOverride None
    Require all granted

    <IfModule headers_module>
        Header always set Cache-Control "max-age=3600"
    </IfModule>
</Directory>

<IfModule ssl_module>
    SSLRandomSeed startup builtin
    SSLRandomSeed connect builtin
</IfModule>

Include "${CAMPUS_ROOT}/conf/extra/httpd-vhosts.conf"
